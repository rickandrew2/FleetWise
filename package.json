{
  "name": "fleetwise",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n DB,API,WEB -c yellow,cyan,magenta \"npm run dev:db\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "down": "docker compose down",
    "dev:db": "docker compose up -d postgres",
    "dev:backend": "mvnw.cmd spring-boot:run",
    "dev:frontend": "npm --prefix fleetwise-web run dev",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "mvnw.cmd test",
    "test:frontend": "npm --prefix fleetwise-web run lint",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "mvnw.cmd -DskipTests package",
    "build:frontend": "npm --prefix fleetwise-web run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}